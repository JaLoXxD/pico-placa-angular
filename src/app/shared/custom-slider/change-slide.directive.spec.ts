import { ChangeSlideDirective } from './change-slide.directive';

describe('ChangeSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeSlideDirective();
    expect(directive).toBeTruthy();
  });
});
